app = "aai-meeting-notes-server"
primary_region = "iad"

[build]

# No [http_service] — this is a worker process, not a web server.
# It connects outbound to LiveKit Cloud via WebSocket.

[[vm]]
  memory = "2gb"
  cpu_kind = "shared"
  cpus = 2
